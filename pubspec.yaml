name: background
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  workmanager: ^0.5.1
  logger: ^2.0.1
  shared_preferences: ^2.2.0
  home_widget:
    git:
      url: https://github.com/HiramOrd/home_widget

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
